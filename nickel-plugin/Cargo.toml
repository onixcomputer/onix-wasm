[package]
name = "nickel-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
nix-wasm-rust = { path = "../nix-wasm-rust" }
nickel-lang-core = { path = "../vendor/nickel-lang-core", default-features = false }
